{
  "name": "heritagelens-ai",
  "version": "1.0.0",
  "description": "HeritageLens AI — Ask the Monument. Evidence-Grounded Visual Intelligence for Karnataka's Chalukyan Heritage.",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build:client": "npm install --prefix client && npm run build --prefix client",
    "build:server": "npm install --prefix server",
    "build": "npm run install:all && npm run build --prefix client",
    "start": "node server/server.js",
    "dev:server": "node --watch server/server.js",
    "dev:client": "npm run dev --prefix client"
  }
}
